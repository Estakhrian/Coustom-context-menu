# Coustom-context-menu

We have a context menu project that disables the default browser behavior when right-clicking and displays a menu with a new style.

ما یک پروژه کانتکست منو داریم که رفتار پیش فرض مرورگر موقع کلیک راست غیر فعال شده و یک منو با استایل جدید نشان داده میشه
